<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state -->
    <item android:color="@color/gSelector"  android:state_checked="true"/>
    <!-- Un‑checked (default) -->
    <item android:color="@color/nonSelectedColor"/>
</selector>