<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <activity
            android:name=".BrowserKitActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.BrowserKit" />

        <activity
            android:name=".ui.player.BrowserPlayerActivity"
            android:exported="false"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.BrowserKit.Fullscreen" />

        <activity
            android:name=".ui.history.BrowserHistoryActivity"
            android:exported="false"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.BrowserKit" />

        <provider
            android:name=".util.BrowserKitFileProvider"
            android:authorities="${applicationId}.browserkit.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/browserkit_file_paths" />
        </provider>

        <receiver
            android:name=".download.BrowserDownloadCancelReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.avd.browserkit.download.ACTION_CANCEL" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
